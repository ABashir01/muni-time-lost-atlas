# Artifacts

This directory is reserved for local acquisition artifacts and other non-source runtime outputs.

Current intended contents:
- `acquisitions/511/operator_active/`
  - timestamped active SFMTA/Muni GTFS zip downloads from `511`
  - adjacent JSON provenance/validation metadata for each downloaded artifact

The acquisition artifacts themselves are gitignored. Keep only lightweight documentation here in version control.
