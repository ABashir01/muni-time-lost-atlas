export function SiteFooter() {
  return (
    <footer className="site-footer">
      <span>Built for riders. Backed by documented fixture payloads for review.</span>
      <span>Typical trip = waiting loss + in-vehicle loss on a full one-way trip.</span>
      <span>Static bundle now, live API integration later.</span>
    </footer>
  );
}
