export function SiteFooter() {
  return (
    <footer className="site-footer">
      <span>Built for riders. Backed by published route-delay evidence.</span>
      <span>Expected rider loss = waiting loss + in-vehicle loss on a full one-way trip.</span>
      <span>The map, rankings, compare, and route pages all read from the same published snapshot.</span>
    </footer>
  );
}
