export function SiteFooter() {
  return (
    <footer className="site-footer">
      <span>Built for riders. Backed by documented API payloads and validated frontend parsing.</span>
      <span>Typical trip = waiting loss + in-vehicle loss on a full one-way trip.</span>
      <span>Historical/static API now, realtime later.</span>
    </footer>
  );
}
